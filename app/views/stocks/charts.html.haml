
%h1 Chart Stocks
= form_tag(:controller => "stocks", :action => "charts", :method => "get") do
  %p
  =label_tag(:stocks, "stock symbols to rate")
  = text_area_tag(:stocks, @stocks)
  %div= submit_tag("View Charts")

%ul
-@watchlists.each do |wl|
  %li
    = link_to wl.name,:controller => 'stocks', :action => 'populate_chartslist', :id => wl.id

-@stock_symbols.each do |stock|
  = image_tag 'http://chart.finance.yahoo.com/z?s=' + stock + '&t=1y&q=l&l=on&z=l&p=e50,e200,p&a=m26-12-9&lang=en-US&region=US'
  %hr
